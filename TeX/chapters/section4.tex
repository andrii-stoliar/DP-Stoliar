% ===================================================================
% Section 4: PI + prepínač pracovných bodov 
% ===================================================================

\section{PI regulácia s prepínaním pracovných bodov}
\label{sec:pi_switching}

% -------------------------------------------------------------------
% Subsection 4.1 - Prepínač pracovných bodov
% -------------------------------------------------------------------

\subsection{Prepínač pracovných bodov}

V tejto časti sa budeme zaoberať návrhom a testovaním prepínača pracovných bodov.  
Prepínač bude predstavovať pomalý regulátor, ktorého úlohou bude zabezpečiť robustné prechody medzi rôznymi pracovnými bodmi systému TS05.  
Cieľom je vytvoriť taký spôsob riadenia, ktorý umožní spoľahlivý presun z ľubovoľného pracovného bodu do ľubovoľného iného pracovného bodu bez neželaných dynamických javov, ako je presýtenie akčného zásahu alebo príliš veľké prekmitové správanie.

Okrem samotného prepínača bola v tejto časti rozšírená aj pracovná oblasť regulácie o dva ďalšie pracovné body systému – pre napätia špirály 2~V a 6~V. Pre každý z týchto pracovných bodov bol postup opakovaný presne podľa postupu uvedeného v sekcii~\ref{sec:identifikacia},  
teda boli:

\begin{itemize}
    \item vykonané merania reakcií systému na jednotkové skoky $\pm 0.5$~V okolo pracovného bodu,
    \item z nameraných údajov bola identifikovaná prenosová funkcia v danom pracovnom bode,
    \item pre každý pracovný bod bol navrhnutý PI regulátor s vlastnými parametrami,
    \item boli určené hodnoty $y_{pb}$ a rozsah výstupu pre $\pm 0.5$~V zmenu vstupu.
\end{itemize}

Prehľad všetkých troch pracovných bodov je uvedený v tabuľke~\ref{tab:pb_overview},  
kde sú uvedené identifikované prenosové funkcie, hodnoty pracovného bodu $y_{pb}$ a parametre jednotlivých PI regulátorov.

\begin{table}[h!]
    \centering
    \caption{Parametre pracovných bodov, identifikované prenosové funkcie a PI regulátory}
    \label{tab:pb_overview}
    \begin{tabular}{|c|c|c|c|c|}
        \hline
        \textbf{$u_{pb}$} & \textbf{Prenosová funkcia} & \textbf{$y_{pb}$} & \textbf{$\Delta y_{pb}$ pri $u_{pb}$ $\pm 0.5$~V} & \textbf{PI parametre} \\ \hline

        2~V &
        $\displaystyle \frac{5.79}{s^2 + 44.21 s + 7.276}$ &
        4.4884 &
        $\pm 0.41$ &
        $K_p = 2.77$, $K_i = 1.48$ \\ \hline

        4~V &
        $\displaystyle \frac{6.074}{s^2 + 59.01 s + 8.95}$ &
        6.3412 &
        $\pm 0.35$ &
        $K_p = 3.58$, $K_i = 1.84$ \\ \hline

        6~V &
        $\displaystyle \frac{7.161}{s^2 + 52.87 s + 21.37}$ &
        7.7441 &
        $\pm 0.18$ &
        $K_p = 1.83$, $K_i = 2.20$ \\ \hline
    \end{tabular}
\end{table}

Keďže pôvodné statické charakteristiky systému boli merané ešte počas letného obdobia, bolo rozhodnuté hodnoty $y_{pb}$ pre tieto experimenty zmerať znova, rovnakým spôsobom ako v sekcii~\ref{sec:staticke_vlastnosti}.  
V tabuľke sú preto uvedené aktuálne hodnoty pracovných bodov, ktoré vernejšie reprezentujú stav systému počas meraní v zimnom období.

% -------------------------------------------------------------------
% Subsubsection 4.1.1 - Opis a schéma riadenia z použitím prepínača
% -------------------------------------------------------------------

\subsubsection{Opis a schéma riadenia z použitím prepínača}

V tejto časti sa prezentuje návrh riadenia systému TS05 s využitím prepínača pracovných bodov.
Zámerom riadenia je pokryť dve odlišné situácie:
\begin{itemize}
    \item reguláciu v okolí zvoleného pracovného bodu.
    \item bezpečný prechod medzi pracovnými bodmi bez neželaných javov, ako je dlhodobé presýtenie akčného zásahu alebo príliš agresívna dynamika.
\end{itemize}

Navrhnutá štruktúra je založená na tom, že pri zmene globálnej žiadanej hodnoty má prioritu prechodový režim (prepínač pracovných bodov),
a až po jeho ukončení sa aktivuje lokálna PI regulácia v okolí aktuálneho pracovného bodu.

Bloková schéma logickej postupnosti operácií navrhnutého algoritmu riadenia je znázornená na obr.~\ref{fig:ts_control_sequence}.

Ako už bolo uvedené, pokiaľ nedôjde ku skokovej zmene globálnej žiadanej hodnoty,
systém pracuje v režime lokálnej regulácie v okolí aktuálneho pracovného bodu.
V tomto prípade je aktívny PI regulátor, ktorý zabezpečuje reguláciu nad normalizovanými veličinami
\[
y_N = y - y_{pb}, \qquad u_N = u - u_{pb},
\]
kde $y_{pb}$ a $u_{pb}$ predstavujú posuny výstupnej a vstupnej veličiny príslušného pracovného bodu.

Informáciu o tom, v ktorom pracovnom bode sa systém nachádza,
nepretržite poskytuje \textit{solver}.
Táto informácia slúži lokálnemu regulátoru na výber zodpovedajúcich posunov $y_{pb}$, $u_{pb}$
a zároveň na nastavenie parametrov PI regulátora $K_p$ a $K_i$.

V prípade, že dôjde ku skokovej zmene globálnej žiadanej hodnoty,
lokálna PI regulácia je dočasne deaktivovaná
a riadenie preberá prepínač pracovných bodov.
Ten zabezpečuje plynulý prechod medzi pracovnými bodmi
a po jeho ukončení sa riadenie automaticky prepne späť
do režimu lokálnej regulácie v okolí nového pracovného bodu.

\begin{center}
    \vbox{
        \makebox[\textwidth][c]{%
            \includegraphics[width=0.85\textwidth]{TS_pbSwitch_block_scheme.pdf}
        }
        \figcaption{Bloková schéma logickej postupnosti operácií riadenia systému TS05}
        \label{fig:ts_control_sequence}
    }
\end{center}

Schéma celého riadenia je uvedená na obr.~\ref{fig:ts_pb_switch_control_scheme}.

Ako je možné vidieť na schéme, rozhodovacia logika je sústredená v bloku \textit{solver},
ktorý na základe globálnej žiadanej hodnoty určuje, či sa systém nachádza v režime prechodu medzi pracovnými bodmi,
alebo už môže prebiehať lokálna regulácia okolo pracovného bodu.
Okrem toho blok \textit{solver} nepretržite určuje aj aktuálny režim \textit{mode\_local},
t.\,j. informáciu o tom, ku ktorému z pracovných bodov (2~V, 4~V alebo 6~V na špirále) má byť priradená lokálna regulácia.

Implementácia bloku \textit{solver} je uvedená v~kóde~\ref{lst:solver_switching_logic}.
Základom je výpočet času, počas ktorého má byť aktívny prechodový režim.
Tento čas sa odvodzuje z veľkosti zmeny globálnej žiadanej hodnoty a z kroku zjemnenia \textit{step\_to\_soft},
pričom sa uvažuje aj dodatočná časová rezerva \textit{extra\_time}.

\begin{center}
    \vbox{
        \makebox[\textwidth][c]{%
            \includegraphics[
                width=1.8\textwidth,
                angle=-270,
                trim=10 220 200 140,
                clip
            ]{TS_pbSwitch_scheme.pdf}
        }
        \figcaption{Schéma riadenia systému TS05 s prepínačom pracovných bodov}
        \label{fig:ts_pb_switch_control_scheme}
    }
\end{center}

\begin{lstlisting}[language=Matlab,
caption={MATLAB implementácia rozhodovacej logiky prepínača pracovných bodov (\textit{solver})},
label={lst:solver_switching_logic}]
function [active_global, active_local, mode_local, sp_global_prevY, switching_time_endY] = solver(time, sp_global, sp_global_prevU, switching_time_endU)

    %Constants
    step_to_soft = 0.005;
    Ts = 0.1;
    extra_time = 10;
    eps = 1e-5;

    active_global = 0;
    active_local = 0;

    %Time of switch working
    if abs(sp_global - sp_global_prevU) > eps
        reg_time = (abs(sp_global - sp_global_prevU) / step_to_soft) * Ts + extra_time;
        reg_time = max(reg_time, extra_time);
        switching_time_endY = time + reg_time;
    else
        switching_time_endY = switching_time_endU;
    end

    %Activators of PI and switch
    if time < switching_time_endY
        active_global = 1;
        active_local = 0;
    else
        active_global = 0;
        active_local = 1;
    end

    if sp_global > 7.6
        mode_local = 6;
    elseif sp_global > 6.2
        mode_local = 4;
    else
        mode_local = 2;
    end

    %memory
    sp_global_prevY = sp_global;

end
\end{lstlisting}

Logika kódu~\ref{lst:solver_switching_logic} vyplýva z výrazu
\begin{equation}
    t_{\mathrm{reg}} =
    \left(\frac{\left|sp_{\mathrm{global}} - sp_{\mathrm{global,prev}}\right|}{\Delta sp}\right) T_s
    + t_{\mathrm{extra}},
    \qquad
    t_{\mathrm{end}} = t + t_{\mathrm{reg}},
    \label{eq:switching_time_rule}
\end{equation}
kde $\Delta sp$ predstavuje krok zjemnenia (\textit{step\_to\_soft}), $T_s$ je vzorkovací čas a $t_{\mathrm{extra}}$ je časová rezerva.
Prechodový režim má v navrhnutej štruktúre prioritu, t.\,j. počas jeho trvania je lokálna PI regulácia zablokovaná.

Po rozhodnutí o režime nasledujú samotné regulátory.
V ľubovoľnom časovom okamihu je aktívny vždy iba jeden z nich:
buď prechodový regulátor (prepínač pracovných bodov), alebo lokálny PI regulátor.
Prepínač pracovných bodov \textit{pb\_switch} realizuje zjemnenie žiadanej hodnoty tak, aby namiesto skoku vznikla rampa,
čo výrazne znižuje riziko presýtenia akčného zásahu pri veľkých zmenách referencie.
Zjemnenie prebieha s krokom $\Delta sp = 0.005$ na jednu iteráciu, t.\,j. požadovaná hodnota sa mení postupne,
kým nedosiahne cieľovú globálnu hodnotu.
Zjemnený setpoint je následne použitý v jednoduchom P regulátore.

Kód bloku \textit{pb\_switch} je uvedený v~kóde~\ref{lst:pb_switch_code}.

\begin{lstlisting}[language=Matlab,
caption={MATLAB implementácia prepínača pracovných bodov (\textit{pb\_switch})},
label={lst:pb_switch_code}]
function [u, sp] = pb_switch(y, active, sp_global, sp_prev)

    %Constants
    Kp = 4;
    Ts = 0.1;
    step_to_soft = 0.005;

    %Logic
    if active

        if abs(sp_global - sp_prev) > step_to_soft
            delta = sign(sp_global - sp_prev) * step_to_soft;
        else
            delta = sp_global - sp_prev;
        end

        sp = sp_prev + delta;
        u = Kp * (sp - y);

    else

        sp = sp_global;
        u = 0;

    end

    u = min(max(u,0), 10);

end
\end{lstlisting}

Čas, počas ktorého je \textit{pb\_switch} aktívny, je priamo viazaný na veľkosť zmeny globálneho setpointu,
čo bolo definované v bloku \textit{solver} vzťahom~\eqref{eq:switching_time_rule}.
Po uplynutí tejto doby sa aktivuje lokálny PI regulátor, ktorý zabezpečuje presné sledovanie žiadanej hodnoty v okolí pracovného bodu.

Lokálna PI regulácia je realizovaná blokom \textit{pi\_block}, ktorý obsahuje dve dôležité úpravy:
\begin{itemize}
    \item ziskové plánovanie (gain scheduling) podľa aktuálne zvoleného pracovného bodu,
    \item sledovanie integrátora aj v neaktívnom stave (tzv. bumpless transfer), aby po opätovnej aktivácii nevznikol neželaný skok akčného zásahu.
\end{itemize}

Ziskové plánovanie spočíva v tom, že podľa signálu \textit{mode\_local} (určeného v \textit{solver}) sa zvolia príslušné parametre
$K_p$, $K_i$ a hodnoty pracovného bodu $(u_{pb}, y_{pb})$.
Normalizácia prebieha odčítaním $y_{pb}$ od meraného výstupu a akčný zásah je limitovaný v rozsahu okolo $u_{pb}$.

Implementácia \textit{pi\_block} je uvedená v~kóde~\ref{lst:pi_block_gs}.
\begin{lstlisting}[language=Matlab,
caption={MATLAB implementácia lokálnej PI regulácie so ziskovým plánovaním a sledovaním integrátora (\textit{pi\_block})},
label={lst:pi_block_gs}]
function [u, integratorY] = pi_block(y, active_local, mode_local, sp_local, u_active, integratorU)

    Ts = 0.1;

    %Gain scheduling
    if mode_local == 2
        pb_u = 2; pb_y = 4.4884;
        Kp = 2.77; Ki = 1.48;
    elseif mode_local == 4
        pb_u = 4; pb_y = 6.3412;
        Kp = 3.58; Ki = 1.84;
    else
        pb_u = 6; pb_y = 7.7441;
        Kp = 1.83; Ki = 2.20;
    end

    u_min = 0 - pb_u;
    u_max = 10 - pb_u;

    yN = y - pb_y;
    e = sp_local - yN;

    if active_local == 0
        uN = u_active - pb_u;
        uN = min(max(uN, u_min), u_max);
        integratorY = (uN - Kp*e) / Ki;
        u = 0;
    else
        integratorY = integratorU + e*Ts;
        uN = Kp*e + Ki*integratorY;
        uN = min(max(uN, u_min), u_max);
        u = uN + pb_u;
    end

end
\end{lstlisting}

Sledovanie integrátora v neaktívnom stave je realizované tak,
že sa z rovnice PI regulátora v normalizovanom tvare
\begin{equation}
    u_N = K_p e + K_i I
    \label{eq:pi_law_normalized}
\end{equation}
vyjadrí stav integrátora
\begin{equation}
    I = \frac{u_N - K_p e}{K_i}.
    \label{eq:integrator_tracking}
\end{equation}
Týmto spôsobom je zabezpečené, že pri prepnutí režimu nedôjde k náhlej zmene vnútornej integrujúcej zložky,
a teda ani k nežiadanému skoku na výstupe regulátora.

Výstupy prechodového regulátora a lokálneho PI regulátora sa následne kombinujú v bloku \textit{normalize\_input},
kde sa vykoná súčet a finálne obmedzenie signálu do rozsahu 0--10~V.
Keďže v každom okamihu je aktívny iba jeden regulátor (druhý dáva nulový výstup),
je táto forma kombinácie v danej štruktúre prípustná a nevedie k nejednoznačnosti riadenia.

Kód bloku \textit{normalize\_input} je uvedený v~kóde~\ref{lst:normalize_input_code}.

\begin{lstlisting}[language=Matlab,
caption={MATLAB implementácia finálnej normalizácie a obmedzenia akčného zásahu (\textit{normalize\_input})},
label={lst:normalize_input_code}]
function u = normalize_input(u_global, u_local)

    u_sum = u_global + u_local;
    u = min(max(u_sum, 0), 10);

end
\end{lstlisting}

% -------------------------------------------------------------------
% Subsubsection 4.1.2 - Experiment s prepínaním pracovných bodov
% -------------------------------------------------------------------

\subsubsection{Experiment s prepínaním pracovných bodov}

V tejto podsekcii bol uskutočnený experiment na reálnom systéme TS05,
ktorého cieľom bolo overiť správnu funkciu navrhnutého algoritmu riadenia
pri prepínaní medzi pracovnými bodmi.
Experiment pozostával z dvoch hlavných fáz.
Najskôr bol systém regulovaný v okolí pracovného bodu pri napätí špirály 4~V,
kde bola realizovaná lokálna skoková zmena žiadanej hodnoty.
Následne došlo k prechodu na pracovný bod 6~V,
po ktorom bola opäť aplikovaná lokálna skoková zmena referencie.

Priebeh experimentu je znázornený na obr.~\ref{fig:ts_pb_switch_experiment},
kde sú zobrazené tri podgrafy.
Prvý podgraf zobrazuje výstupnú veličinu systému spolu so žiadanou hodnotou,
pričom žiadaná hodnota je v tomto prípade normalizovaná
za účelom prehľadnejšej vizualizácie priebehu regulácie.
Skutočné (nenormalizované) hodnoty globálneho a lokálneho setpointu
sú uvedené v treťom podgrafe.

\begin{center}
    \vbox{
        \makebox[\textwidth][c]{%
            \includegraphics[width=1.5\textwidth]{ts_ml_switch_1st.png}
        }
        \figcaption{Experiment s prepínaním pracovných bodov: výstup systému, akčné zásahy a žiadané hodnoty}
        \label{fig:ts_pb_switch_experiment}
    }
\end{center}

Z priebehu akčného zásahu na druhom podgrafe je zrejmé,
že riadiaci signál počas celého experimentu nenaráža na svoje maximálne obmedzenie,
čo svedčí o vhodne zvolenej stratégii regulácie.
Zároveň je možné pozorovať, že v každom okamihu je aktívny iba jeden akčný zásah:
ak pracuje lokálny PI regulátor, výstup prepínača pracovných bodov je nulový
a naopak.
Týmto sa potvrdzuje správna sekvenčná činnosť jednotlivých blokov riadenia,
presne v súlade s návrhom algoritmu.

Dôležitým pozorovaním je aj správanie systému pri prepnutí z prechodového režimu
na lokálnu PI reguláciu.
Po aktivácii lokálneho regulátora nedochádza k náhlemu „výstrelu“ akčného zásahu,
čo indikuje správnu funkciu sledovania stavu integrátora.
Integrujúca zložka regulátora je teda pri prepínaní režimov v konzistentnom stave,
čo výrazne zvyšuje kvalitu prechodu medzi režimami riadenia.

V regulácii sa súčasne využíva lokálny setpoint
a zjemnený globálny setpoint generovaný prepínačom pracovných bodov.

Napriek celkovo uspokojivému správaniu systému sa počas prepínania pracovných bodov
objavuje aj jeden nežiaduci jav.
Keďže pred samotným prepnutím pracuje lokálny PI regulátor,
po aktivácii prepínača pracovných bodov je akčný zásah prepínača
v počiatočnom okamihu nulový a následne pomerne rýchlo narastá.
Tento prechodný jav spôsobí krátkodobý pokles výstupnej veličiny systému,
ktorý je síce časovo obmedzený, avšak pozorovateľný v nameraných dátach. Zároveň je možné pozorovať, že keďže prepínač pracovných bodov je realizovaný
vo forme P regulátora, v jeho činnosti sa vyskytuje malá trvalá regulačná odchýlka.
Tento nedostatok by bolo možné v budúcnosti odstrániť
rozšírením regulátora o integračnú zložku.

V ďalšej fáze práce budú tieto problémy riešené
a navrhnutý algoritmus riadenia bude implementovaný na ďalších platformách.